{
  "name": "hipaa-security-kit",
  "version": "0.1.0",
  "description": "HIPAA-compliant security toolkit for Supabase applications",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "turbo run test",
    "build": "turbo run build",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
